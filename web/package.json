{
  "name": "m5stack-lyrebird-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "sirv build --host 0.0.0.0 --port ${PORT:-3000} --etag",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "firmware": "../scripts/build-firmware.sh"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "@types/w3c-web-serial": "^1.0.8",
    "svelte": "^4.2.7",
    "svelte-check": "^3.6.0",
    "tslib": "^2.4.1",
    "typescript": "^5.0.0",
    "vite": "^5.0.3"
  },
  "dependencies": {
    "esptool-js": "^0.6.1",
    "sirv-cli": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
